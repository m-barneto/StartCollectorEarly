export interface ModConfig {
    startingLevel: number;
    hideFindItemTasks: boolean;
    removeFoundInRaidRequirement: boolean;
    prerequisiteQuestCompletionRequired: boolean;
    prerequisiteQuestCompletionVisibility: boolean;
}